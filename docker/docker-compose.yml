services:
  ollama:
    build:
      context: ollama
      dockerfile: Dockerfile
    container_name: ollama
    ports:
      - "11434:11434"

  piper:
    build:
      context: piper
      dockerfile: Dockerfile
    environment:
      - PIPER_VOICE=en_US-hfc_female-medium
    container_name: piper
    ports:
      - "5000:5000"    

    